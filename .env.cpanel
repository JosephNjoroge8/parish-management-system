# Parish Management System - cPanel Deployment Configuration

## Production Environment Variables
# Copy this to .env in your cPanel deployment

APP_NAME="Parish Management System"
APP_ENV=production
APP_KEY=base64:YOUR_APP_KEY_HERE  # Generate with: php artisan key:generate
APP_DEBUG=false
APP_TIMEZONE=UTC
APP_URL=https://yourdomain.com

# Database Configuration (adjust for your cPanel MySQL)
DB_CONNECTION=mysql
DB_HOST=localhost
DB_PORT=3306
DB_DATABASE=your_database_name
DB_USERNAME=your_database_user
DB_PASSWORD=your_database_password

# Cache Configuration
BROADCAST_DRIVER=log
CACHE_DRIVER=file
FILESYSTEM_DISK=local
QUEUE_CONNECTION=sync
SESSION_DRIVER=file
SESSION_LIFETIME=120

# Mail Configuration (adjust for your hosting provider)
MAIL_MAILER=smtp
MAIL_HOST=mail.yourdomain.com
MAIL_PORT=587
MAIL_USERNAME=noreply@yourdomain.com
MAIL_PASSWORD=your_email_password
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=noreply@yourdomain.com
MAIL_FROM_NAME="Parish Management System"

# Security Settings
SESSION_ENCRYPT=true
BCRYPT_ROUNDS=12

# Production Optimization
CACHE_PREFIX=parish_
SESSION_COOKIE_SECURE=true
SESSION_COOKIE_HTTPONLY=true
SESSION_SAME_SITE=strict

# Asset Settings
ASSET_URL=https://yourdomain.com
MIX_ASSET_URL=https://yourdomain.com

# Logging
LOG_CHANNEL=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=warning

# Performance Settings
OPCACHE_ENABLE=true
QUEUE_FAILED_DRIVER=database-uuids